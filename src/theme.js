import { ThemeProvider, createTheme } from '@mui/material';

export const theme = createTheme({
    pallete: {
        primary: {
            main: "red",
            light:"gray"
        },
        secondary: {
            main: "#FEEFEC",
            light:"gray"
        }
    }

})